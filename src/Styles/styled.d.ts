import 'styled-components/native';

declare module 'components' {
    export interface DefaultTheme {
        color: {
            main: string;
            white: string;
            bg: string;
        };
    }
}